export interface NavLinks {
  url: string;
  text: string;
}

export interface ServiceCardType {
  img: string;
  url: string;
  text: string;
  title: string;
}

export interface TestimonialCardType {
  review: string;
  client: string;
}
